// No longer used. The admin panel dropped the marketing-style footer
// in favor of a persistent sidebar (see AdminSidebar.jsx) — a
// dashboard doesn't need a footer with policy links the way the
// public site does.
//
// Kept as an empty stub instead of being deleted outright — feel free
// to delete this file from disk.
export default function AdminFooter() {
  return null;
}
