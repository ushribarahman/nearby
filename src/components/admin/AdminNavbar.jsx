// No longer used. The admin panel now uses a persistent sidebar
// instead of a top navbar — see components/admin/AdminSidebar.jsx and
// layouts/AdminLayout.jsx.
//
// Kept as an empty stub instead of being deleted outright — feel free
// to delete this file from disk.
export default function AdminNavbar() {
  return null;
}
